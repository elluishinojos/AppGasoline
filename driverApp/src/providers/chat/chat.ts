import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Socket } from 'ng-socket-io';

/*
  Generated class for the ChatProvider provider.

  See https://angular.io/guide/dependency-injection for more info on providers
  and Angular DI.
*/
@Injectable()
export class ChatProvider {

  // constructor() {
  // }

  // coonnect(){
    
  // }

  // sendMessage(msg: string) {
  //   this.socket.emit("chat message", msg);
  // }

  // sendCurrentLocation(msg: string) {
  //   this.socket.emit("currentLocation", msg);
  // }

  // sendConnection(msg: string) {
  //   this.socket.emit("online/offline", msg);
  // }
  // sendStage1(msg: string) {
  //   this.socket.emit("stage1", msg);
  // }
  // sendStage2(msg: string) {
  //   this.socket.emit("stage2", msg);
  // }
  // sendStage3(msg: string) {
  //   this.socket.emit("stage3", msg);
  // }

  // sendStage4(msg: string) {
  //   this.socket.emit("stage4", msg);
  // }

  // getStage1(msg: string) {
  //   this.socket.on("stage1",function (msg) {
  //     return msg.toString();
  //   });
  // }
  // getStage2(msg: string) {
  //   this.socket.on("stage2",function (msg) {
  //     return msg.toString();
  //   });
  // }
  // getStage3(msg: string) {
  //   this.socket.on("stage3",function (msg) {
  //     return msg.toString();
  //   });
  // }
  // getStage4(msg: string) {
  //   this.socket.on("stage4",function (msg) {
  //     return msg.toString();
  //   });
  // }

  // getOrder() {
  //   this.socket.on('newOrders', function (msg) {
  //     return msg.toString();
  //   });
  // }

  // getMessage() {
  //   return this.socket
  //     .fromEvent<any>("chat message");
  // }

  // close() {
  //   this.socket.disconnect();
  // }

}
